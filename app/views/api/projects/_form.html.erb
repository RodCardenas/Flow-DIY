<%= form_for(@api_project) do |f| %>
  <% if @api_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_project.errors.count, "error") %> prohibited this api_project from being saved:</h2>

      <ul>
      <% @api_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
